# munchiegames

## A simple suite of math focused games for kids

Goal is to fit into a small footprint, few if any external libraries, all native Javascript, HTML and CSS.

Simple games, fast downloads for kids

Run with ```docker compose up -d --build
